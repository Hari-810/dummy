| **Test Case**                           | **Input**                                                                                                   | **Output**                                                                                        | **Purpose**                                                                                                                                            |
| --------------------------------------- | ----------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `test_data_generator_tabular_success`   | JSON payload with tabular data generation parameters (`user_request`, `sampleCount`, `schemaDetails`, etc.) | JSON response with keys: `json` (list of records), `file_name` (string), `file_byte` (base64 str) | Verify that the tabular data generator endpoint returns a successful response with correctly formatted data, mocking data generation and file reading. |
| `test_data_generator_textual_success`   | JSON payload with textual data generation parameters (`userQuery`, `sampleCount`, `textStyle`, etc.)        | JSON response with keys: `json` (string text), `file_name` (string), `file_byte` (base64 str)     | Verify that the textual data generator endpoint returns a successful response with expected text output, mocking data generation and file reading.     |
| `test_data_generator_tabular_exception` | JSON payload for tabular data generation, but generator method patched to raise Exception                   | HTTP 500 response with detail containing "Processing failed"                                      | Confirm that the tabular data generator endpoint correctly handles exceptions and returns HTTP 500 status with error details.                          |
| `test_data_generator_textual_exception` | JSON payload for textual data generation, but generator method patched to raise Exception                   | HTTP 500 response with detail containing "Processing failed"                                      | Confirm that the textual data generator endpoint correctly handles exceptions and returns HTTP 500 status with error details.                          |

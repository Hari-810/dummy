| **Test Case**                     | **Input (Fixture)**                                           | **Expected Output**                                                                                                                                                                                 | **Purpose**                                                                                                                                                                       |
| --------------------------------- | ------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `test_jumbling_mixed_types`       | A CSV with `Name (str)`, `Age (int)`, `City (str)`            | JSON response with:<br>✅ `summary.schema_match = True`<br>✅ `summary.dtype_match = True`<br>✅ Column-level stats (`Percent Changed`, `Jaccard` or `Correlation`, etc.)<br>✅ Valid `inferences` dict | ✔️ Validates shuffling and analysis for a mixed data type file<br>✔️ Checks if schema and dtypes are retained<br>✔️ Ensures numeric and categorical columns get appropriate stats |
| `test_numeric_column_analysis`    | A CSV with only numeric columns `X`, `Y`                      | `inferences.numeric_column_stats` with:<br>✅ `correlation`, `ks_statistic`, `p_value`, `interpretation`, `distribution_comment` for each column                                                     | ✔️ Ensures statistical metrics are generated correctly for numeric columns<br>✔️ Confirms the API returns interpretability metrics for shuffled numeric data                      |
| `test_duplicate_value_handling`   | A CSV with duplicate values in `Category` and `Value` columns | `inferences.categorical_jaccard` includes valid similarity values (between 0 and 1)                                                                                                                 | ✔️ Checks that shuffling doesn't break with duplicate values<br>✔️ Ensures similarity measures are meaningful even with redundancy                                                |
| `test_dtype_and_schema_integrity` | Same as `mixed_csv`                                           | `summary.schema_match = True`<br>`dtype_match = True`<br>`dtype_details[col] = True` for each column                                                                                                | ✔️ Verifies schema and datatype consistency is preserved after jumbling<br>✔️ Double-checks that summary-level metadata is correct                                                |
